apiVersion: v1
kind: Service
metadata:
  name: secrets-test-1
  namespace: default
  labels:
    app: secrets-test-1
spec:
  type: ClusterIP
  selector:
    app: secrets-test-1
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: secrets-test-1
  namespace: default
  labels:
    app: secrets-test-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secrets-test-1
  template:
    metadata:
      labels:
        app: secrets-test-1
    spec:
      containers:
        - name: secrets-test-1-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 400m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-judge0-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: secrets-test-1
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: secretsqa-r9tag.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: secrets-test-1
                port:
                  number: 3000
  tls:
    - hosts:
        - secretsqa-r9tag.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
