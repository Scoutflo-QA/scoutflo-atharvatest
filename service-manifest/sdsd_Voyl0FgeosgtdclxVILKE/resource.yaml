apiVersion: v1
kind: Service
metadata:
  name: thrthnbsd
  namespace: test-cert
  labels:
    app: thrthnbsd
spec:
  type: ClusterIP
  selector:
    app: thrthnbsd
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: thrthnbsd
  namespace: test-cert
  labels:
    app: thrthnbsd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: thrthnbsd
  template:
    metadata:
      labels:
        app: thrthnbsd
    spec:
      containers:
        - name: thrthnbsd-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: thrthnbsd
  namespace: test-cert
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: uwkbkjb.scoutflo.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: thrthnbsd
                port:
                  number: 3000
    - host: dont-wait-list-rhurf.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: thrthnbsd
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-rhurf.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
