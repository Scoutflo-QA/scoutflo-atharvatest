apiVersion: v1
kind: Service
metadata:
  name: deiutgbrebi
  namespace: test-cert
  labels:
    app: deiutgbrebi
spec:
  type: ClusterIP
  selector:
    app: deiutgbrebi
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deiutgbrebi
  namespace: test-cert
  labels:
    app: deiutgbrebi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deiutgbrebi
  template:
    metadata:
      labels:
        app: deiutgbrebi
    spec:
      containers:
        - name: deiutgbrebi-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: deiutgbrebi
  namespace: test-cert
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: vwethbryhyje.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: deiutgbrebi
                port:
                  number: 3000
    - host: dont-wait-list-8qpzk.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: deiutgbrebi
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-8qpzk.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
