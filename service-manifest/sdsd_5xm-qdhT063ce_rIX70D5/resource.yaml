apiVersion: v1
kind: Service
metadata:
  name: asdasdas
  namespace: athunew
  labels:
    app: asdasdas
spec:
  type: ClusterIP
  selector:
    app: asdasdas
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: asdasdas
  namespace: athunew
  labels:
    app: asdasdas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: asdasdas
  template:
    metadata:
      labels:
        app: asdasdas
    spec:
      containers:
        - name: asdasdas-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env: []
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: asdasdas
  namespace: athunew
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: nmoiooio-zy2xq.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: asdasdas
                port:
                  number: 80
  tls:
    - hosts:
        - nmoiooio-zy2xq.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
