apiVersion: v1
kind: Service
metadata:
  name: waitlistprodab23
  namespace: athunew
  labels:
    app: waitlistprodab23
spec:
  type: ClusterIP
  selector:
    app: waitlistprodab23
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: waitlistprodab23
  namespace: athunew
  labels:
    app: waitlistprodab23
spec:
  replicas: 1
  selector:
    matchLabels:
      app: waitlistprodab23
  template:
    metadata:
      labels:
        app: waitlistprodab23
    spec:
      containers:
        - name: waitlistprodab23-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 200m
            limits:
              memory: 1024Mi
              cpu: 400m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: waitlistprodab23
  namespace: athunew
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: waitlist-7yamk.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: waitlistprodab23
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlist-7yamk.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
