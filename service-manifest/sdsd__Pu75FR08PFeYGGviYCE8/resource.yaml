apiVersion: v1
kind: Service
metadata:
  name: qa2
  namespace: default
  labels:
    app: qa2
spec:
  type: ClusterIP
  selector:
    app: qa2
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qa2
  namespace: default
  labels:
    app: qa2
spec:
  replicas: 3
  selector:
    matchLabels:
      app: qa2
  template:
    metadata:
      labels:
        app: qa2
    spec:
      containers:
        - name: qa2-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qa2
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: scan-to-prod-today-2-fabc7.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: qa2
                port:
                  number: 3000
  tls:
    - hosts:
        - scan-to-prod-today-2-fabc7.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
