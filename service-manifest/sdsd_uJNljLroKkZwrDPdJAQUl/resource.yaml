apiVersion: v1
kind: Service
metadata:
  name: final-ssl-test
  namespace: scoutflo-monitoring
  labels:
    app: final-ssl-test
spec:
  type: ClusterIP
  selector:
    app: final-ssl-test
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: final-ssl-test
  namespace: scoutflo-monitoring
  labels:
    app: final-ssl-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: final-ssl-test
  template:
    metadata:
      labels:
        app: final-ssl-test
    spec:
      containers:
        - name: final-ssl-test-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: final-ssl-test
  namespace: scoutflo-monitoring
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: final-ssl-test.rkjangidbuilder.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: final-ssl-test
                port:
                  number: 3000
    - host: dont-wait-list-u7hua.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: final-ssl-test
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-u7hua.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
    - hosts:
        - final-ssl-test.rkjangidbuilder.com
      secretName: wildcard-z07004282wlzt96eag2kr-tls
