apiVersion: v1
kind: Service
metadata:
  name: ware-creek
  namespace: default
  labels:
    app: ware-creek
spec:
  type: ClusterIP
  selector:
    app: ware-creek
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ware-creek
  namespace: default
  labels:
    app: ware-creek
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ware-creek
  template:
    metadata:
      labels:
        app: ware-creek
    spec:
      containers:
        - name: ware-creek-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ware-creek
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlist2-jjplj.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ware-creek
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlist2-jjplj.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
