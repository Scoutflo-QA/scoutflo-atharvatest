apiVersion: v1
kind: Service
metadata:
  name: vbervberkvb
  namespace: default
  labels:
    app: vbervberkvb
spec:
  type: ClusterIP
  selector:
    app: vbervberkvb
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vbervberkvb
  namespace: default
  labels:
    app: vbervberkvb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vbervberkvb
  template:
    metadata:
      labels:
        app: vbervberkvb
    spec:
      containers:
        - name: vbervberkvb-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vbervberkvb
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: verbnvierb.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vbervberkvb
                port:
                  number: 3000
    - host: dont-wait-list-jkvy1.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vbervberkvb
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-jkvy1.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
