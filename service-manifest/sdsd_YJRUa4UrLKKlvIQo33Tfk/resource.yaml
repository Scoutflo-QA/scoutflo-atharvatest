apiVersion: v1
kind: Service
metadata:
  name: abprode
  namespace: default
  labels:
    app: abprode
spec:
  type: ClusterIP
  selector:
    app: abprode
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: abprode
  namespace: default
  labels:
    app: abprode
spec:
  replicas: 3
  selector:
    matchLabels:
      app: abprode
  template:
    metadata:
      labels:
        app: abprode
    spec:
      containers:
        - name: abprode-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: abprode
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-slcuz.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: abprode
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-slcuz.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
