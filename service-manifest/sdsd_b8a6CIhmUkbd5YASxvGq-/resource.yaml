apiVersion: v1
kind: Service
metadata:
  name: validchecks8j
  namespace: athunew
  labels:
    app: validchecks8j
spec:
  type: ClusterIP
  selector:
    app: validchecks8j
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: validchecks8j
  namespace: athunew
  labels:
    app: validchecks8j
spec:
  replicas: 1
  selector:
    matchLabels:
      app: validchecks8j
  template:
    metadata:
      labels:
        app: validchecks8j
    spec:
      containers:
        - name: validchecks8j-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 200m
            limits:
              memory: 1024Mi
              cpu: 400m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: validchecks8j
  namespace: athunew
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: waitlist-tixd.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: validchecks8j
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlist-tixd.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
