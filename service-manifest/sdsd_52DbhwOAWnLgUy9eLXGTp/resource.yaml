apiVersion: v1
kind: Service
metadata:
  name: namespacetest2
  namespace: newname
  labels:
    app: namespacetest2
spec:
  type: ClusterIP
  selector:
    app: namespacetest2
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: namespacetest2
  namespace: newname
  labels:
    app: namespacetest2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: namespacetest2
  template:
    metadata:
      labels:
        app: namespacetest2
    spec:
      containers:
        - name: namespacetest2-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: namespacetest2
  namespace: newname
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: namespacetest-ydfga.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: namespacetest2
                port:
                  number: 3000
  tls:
    - hosts:
        - namespacetest-ydfga.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
