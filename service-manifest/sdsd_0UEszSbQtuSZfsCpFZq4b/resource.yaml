apiVersion: v1
kind: Service
metadata:
  name: cloudform
  namespace: default
  labels:
    app: cloudform
spec:
  type: ClusterIP
  selector:
    app: cloudform
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudform
  namespace: default
  labels:
    app: cloudform
spec:
  replicas: 3
  selector:
    matchLabels:
      app: cloudform
  template:
    metadata:
      labels:
        app: cloudform
    spec:
      containers:
        - name: cloudform-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloudform
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: blueocean-5wz9q.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloudform
                port:
                  number: 3000
  tls:
    - hosts:
        - blueocean-5wz9q.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
