apiVersion: v1
kind: Service
metadata:
  name: test-cert-three
  namespace: test-cert
  labels:
    app: test-cert-three
spec:
  type: ClusterIP
  selector:
    app: test-cert-three
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-cert-three
  namespace: test-cert
  labels:
    app: test-cert-three
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-cert-three
  template:
    metadata:
      labels:
        app: test-cert-three
    spec:
      containers:
        - name: test-cert-three-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-cert-three
  namespace: test-cert
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-cert-three.scoutflo.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-cert-three
                port:
                  number: 3000
    - host: dont-wait-list-ns1mg.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-cert-three
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-ns1mg.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
