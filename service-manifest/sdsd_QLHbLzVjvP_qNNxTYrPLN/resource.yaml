apiVersion: v1
kind: Service
metadata:
  name: scantesttest2323
  namespace: default
  labels:
    app: scantesttest2323
spec:
  type: ClusterIP
  selector:
    app: scantesttest2323
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scantesttest2323
  namespace: default
  labels:
    app: scantesttest2323
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scantesttest2323
  template:
    metadata:
      labels:
        app: scantesttest2323
    spec:
      containers:
        - name: scantesttest2323-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-dockerwaitlist-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: scantesttest2323
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: atharvaprodtest-23pfk.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: scantesttest2323
                port:
                  number: 3000
  tls:
    - hosts:
        - atharvaprodtest-23pfk.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
