apiVersion: v1
kind: Service
metadata:
  name: monaco
  namespace: default
  labels:
    app: monaco
spec:
  type: ClusterIP
  selector:
    app: monaco
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: monaco
  namespace: default
  labels:
    app: monaco
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monaco
  template:
    metadata:
      labels:
        app: monaco
    spec:
      containers:
        - name: monaco-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 200m
            limits:
              memory: 1024Mi
              cpu: 400m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monaco
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-wer5a.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monaco
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-wer5a.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
