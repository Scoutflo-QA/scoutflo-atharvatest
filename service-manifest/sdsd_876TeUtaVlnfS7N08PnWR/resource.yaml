apiVersion: v1
kind: ServiceAccount
metadata:
  name: scoutflo-sa-validchecks9mm
  namespace: default
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::891376929531:role/Scoutflo-cloud-credentials-3272ca00-3b68-4272-8e2d-90327b6c3965

---
apiVersion: v1
kind: Service
metadata:
  name: validchecks9mm
  namespace: default
  labels:
    app: validchecks9mm
spec:
  type: ClusterIP
  selector:
    app: validchecks9mm
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: validchecks9mm
  namespace: default
  labels:
    app: validchecks9mm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: validchecks9mm
  template:
    metadata:
      labels:
        app: validchecks9mm
    spec:
      serviceAccountName: scoutflo-sa-validchecks9mm
      containers:
        - name: validchecks9mm-container
          image: 891376929531.dkr.ecr.ap-south-2.amazonaws.com/waitlist-private:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: validchecks9mm
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlist-ecr-9b55i.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: validchecks9mm
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlist-ecr-9b55i.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
