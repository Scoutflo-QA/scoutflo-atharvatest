apiVersion: v1
kind: Service
metadata:
  name: vedu-test
  namespace: test-certificate
  labels:
    app: vedu-test
spec:
  type: ClusterIP
  selector:
    app: vedu-test
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vedu-test
  namespace: test-certificate
  labels:
    app: vedu-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vedu-test
  template:
    metadata:
      labels:
        app: vedu-test
    spec:
      containers:
        - name: vedu-test-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vedu-test
  namespace: test-certificate
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: vedu-test.scoutflo.services
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vedu-test
                port:
                  number: 3000
    - host: dont-wait-list-jinqj.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vedu-test
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-jinqj.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
    - hosts:
        - vedu-test.scoutflo.services
      secretName: wildcard-z06448543vcs2ukhgxk1w-tls
