apiVersion: v1
kind: ServiceAccount
metadata:
  name: scoutflo-sa-dockerscan
  namespace: default
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::891376929531:role/Scoutflo-cloud-credentials-3272ca00-3b68-4272-8e2d-90327b6c3965

---
apiVersion: v1
kind: Service
metadata:
  name: dockerscan
  namespace: default
  labels:
    app: dockerscan
spec:
  type: ClusterIP
  selector:
    app: dockerscan
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dockerscan
  namespace: default
  labels:
    app: dockerscan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dockerscan
  template:
    metadata:
      labels:
        app: dockerscan
    spec:
      serviceAccountName: scoutflo-sa-dockerscan
      containers:
        - name: dockerscan-container
          image: 891376929531.dkr.ecr.ap-south-2.amazonaws.com/waitlist-private:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dockerscan
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: waitlistfinal-edgjn.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dockerscan
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistfinal-edgjn.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
