apiVersion: v1
kind: Service
metadata:
  name: dfgrtnetyntynyyt
  namespace: test-certificate
  labels:
    app: dfgrtnetyntynyyt
spec:
  type: ClusterIP
  selector:
    app: dfgrtnetyntynyyt
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dfgrtnetyntynyyt
  namespace: test-certificate
  labels:
    app: dfgrtnetyntynyyt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dfgrtnetyntynyyt
  template:
    metadata:
      labels:
        app: dfgrtnetyntynyyt
    spec:
      containers:
        - name: dfgrtnetyntynyyt-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dfgrtnetyntynyyt
  namespace: test-certificate
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-ftbau.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dfgrtnetyntynyyt
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-ftbau.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
