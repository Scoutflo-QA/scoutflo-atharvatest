apiVersion: v1
kind: Service
metadata:
  name: test-commit
  namespace: default
  labels:
    app: test-commit
spec:
  type: ClusterIP
  selector:
    app: test-commit
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-commit
  namespace: default
  labels:
    app: test-commit
spec:
  replicas: 3
  selector:
    matchLabels:
      app: test-commit
  template:
    metadata:
      labels:
        app: test-commit
    spec:
      containers:
        - name: test-commit-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 256Mi
              cpu: 200m
            limits:
              memory: 512Mi
              cpu: 400m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-dockerwaitlist-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-commit
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: test-upsert-docker-nhtrk.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-commit
                port:
                  number: 3000
  tls:
    - hosts:
        - test-upsert-docker-nhtrk.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
