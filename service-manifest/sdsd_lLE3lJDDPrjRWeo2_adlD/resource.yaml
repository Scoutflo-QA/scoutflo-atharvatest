apiVersion: v1
kind: Service
metadata:
  name: testnamespacejoke
  namespace: default
  labels:
    app: testnamespacejoke
spec:
  type: ClusterIP
  selector:
    app: testnamespacejoke
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: testnamespacejoke
  namespace: default
  labels:
    app: testnamespacejoke
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testnamespacejoke
  template:
    metadata:
      labels:
        app: testnamespacejoke
    spec:
      containers:
        - name: testnamespacejoke-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 200m
            limits:
              memory: 1024Mi
              cpu: 400m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyamp07-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: testnamespacejoke
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: scoutflohbn-zyvrw.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: testnamespacejoke
                port:
                  number: 3000
  tls:
    - hosts:
        - scoutflohbn-zyvrw.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
