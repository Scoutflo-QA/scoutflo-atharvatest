apiVersion: v1
kind: Service
metadata:
  name: athunewtest
  namespace: default
  labels:
    app: athunewtest
spec:
  type: ClusterIP
  selector:
    app: athunewtest
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: athunewtest
  namespace: default
  labels:
    app: athunewtest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: athunewtest
  template:
    metadata:
      labels:
        app: athunewtest
    spec:
      containers:
        - name: athunewtest-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-judge0-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: athunewtest
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: athu-h9va7.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: athunewtest
                port:
                  number: 3000
  tls:
    - hosts:
        - athu-h9va7.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
