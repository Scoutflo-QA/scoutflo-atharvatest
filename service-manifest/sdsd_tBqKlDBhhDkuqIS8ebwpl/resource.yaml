apiVersion: v1
kind: Service
metadata:
  name: asdf
  namespace: default
  labels:
    app: asdf
spec:
  type: ClusterIP
  selector:
    app: asdf
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: asdf
  namespace: default
  labels:
    app: asdf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: asdf
  template:
    metadata:
      labels:
        app: asdf
    spec:
      containers:
        - name: asdf-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 200m
            limits:
              memory: 1024Mi
              cpu: 400m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-new-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: asdf
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: waitlist-bonuf.scoutflo.agency
      http:
        paths:
          - path: /service-paths
            pathType: Prefix
            backend:
              service:
                name: asdf
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlist-bonuf.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
