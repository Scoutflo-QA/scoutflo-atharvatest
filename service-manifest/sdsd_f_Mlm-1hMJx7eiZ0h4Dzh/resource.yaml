apiVersion: v1
kind: Service
metadata:
  name: tlscheck
  namespace: default
  labels:
    app: tlscheck
spec:
  type: ClusterIP
  selector:
    app: tlscheck
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tlscheck
  namespace: default
  labels:
    app: tlscheck
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tlscheck
  template:
    metadata:
      labels:
        app: tlscheck
    spec:
      containers:
        - name: tlscheck-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tlscheck
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: dont-wait-list-dipny.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tlscheck
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-dipny.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
