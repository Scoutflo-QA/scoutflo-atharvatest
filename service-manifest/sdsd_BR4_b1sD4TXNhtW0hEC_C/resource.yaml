apiVersion: v1
kind: Service
metadata:
  name: validchecks109
  namespace: default
  labels:
    app: validchecks109
spec:
  type: ClusterIP
  selector:
    app: validchecks109
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: validchecks109
  namespace: default
  labels:
    app: validchecks109
spec:
  replicas: 1
  selector:
    matchLabels:
      app: validchecks109
  template:
    metadata:
      labels:
        app: validchecks109
    spec:
      containers:
        - name: validchecks109-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NODE_ENV
              value: production
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: validchecks109
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlist2-rqwtp.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: validchecks109
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlist2-rqwtp.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
