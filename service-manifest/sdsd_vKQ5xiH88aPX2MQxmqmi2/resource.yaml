apiVersion: v1
kind: Service
metadata:
  name: mlmimlin
  namespace: test-cert
  labels:
    app: mlmimlin
spec:
  type: ClusterIP
  selector:
    app: mlmimlin
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlmimlin
  namespace: test-cert
  labels:
    app: mlmimlin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mlmimlin
  template:
    metadata:
      labels:
        app: mlmimlin
    spec:
      containers:
        - name: mlmimlin-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 250m
            limits:
              memory: 1024Mi
              cpu: 500m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-the-latest-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mlmimlin
  namespace: test-cert
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: jljlhnkjn.scoutflo.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mlmimlin
                port:
                  number: 3000
    - host: dont-wait-list-z3jhz.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mlmimlin
                port:
                  number: 3000
  tls:
    - hosts:
        - dont-wait-list-z3jhz.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
