apiVersion: v1
kind: Service
metadata:
  name: tlasss2
  namespace: default
  labels:
    app: tlasss2
spec:
  type: ClusterIP
  selector:
    app: tlasss2
  ports:
    - name: https
      protocol: TCP
      port: 3000
      targetPort: 3000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tlasss2
  namespace: default
  labels:
    app: tlasss2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tlasss2
  template:
    metadata:
      labels:
        app: tlasss2
    spec:
      containers:
        - name: tlasss2-container
          image: satyamp07/waitlist:latest
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: 512Mi
              cpu: 200m
            limits:
              memory: 1024Mi
              cpu: 400m
          env:
            - name: NEXT_PUBLIC_ATLAS_BACKEND
              value: https://api.atlas.scoutflo.com/api
      imagePullSecrets:
        - name: scoutflo-satyam-docker-docker

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tlasss2
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  rules:
    - host: waitlistab-nawmi.scoutflo.agency
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tlasss2
                port:
                  number: 3000
  tls:
    - hosts:
        - waitlistab-nawmi.scoutflo.agency
      secretName: scoutflo-agency-wildcard-tls
